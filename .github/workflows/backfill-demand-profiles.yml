name: Production Profile Backfill Ownership Notice

on:
  workflow_dispatch:

jobs:
  production-owner:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the single production analysis chain
        run: |
          echo "Production profile backfill is owned by the Alibaba Cloud technexus-sync service."
          echo "Run analyze_demands.py on the server so results are written to the live SQLite database."
          echo "AI bulk backfill remains opt-in to avoid uncontrolled API cost."
