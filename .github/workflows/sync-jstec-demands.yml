name: Production Demand Sync Ownership Notice

on:
  workflow_dispatch:

jobs:
  production-owner:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the single production sync chain
        run: |
          echo "Production demand synchronization no longer runs in GitHub Actions."
          echo "The Alibaba Cloud technexus-sync.timer writes directly to the one live SQLite database."
          echo "This prevents the previous DATABASE_URL database from diverging from production."
