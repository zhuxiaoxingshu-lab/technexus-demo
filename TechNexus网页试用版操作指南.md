# TechNexus 技术经理人网页试用版操作指南

## 1. 启动

进入目录：

```powershell
C:\Users\xdh\Desktop\工作流\技术需求爬取
```

双击：

```text
启动TechNexus试用版.bat
```

或在 PowerShell 中执行：

```powershell
python technexus_app\app.py
```

启动成功后，浏览器会自动打开：

```text
http://127.0.0.1:8010/
```

如果浏览器没有自动打开，手动复制上面的地址到浏览器即可。

## 2. 前台试用流程

1. 在首页填写一个技术成果示例，点击“查看匹配示例”。
2. 或进入“成果提交”，填写成果名称、技术领域、应用场景、成果摘要、成熟度等信息。
3. 选择匹配方式：
   - “快速匹配”：不调用 DeepSeek API，仅用本地规则模糊匹配。
   - “AI智能匹配”：调用 DeepSeek API，对候选需求做精排并生成理由。
4. 点击“开始AI智能匹配”或“开始快速匹配”。
5. 在“匹配结果”页面查看最多 5 条匹配需求、彩色匹配分、四个维度分数、匹配理由和合作建议。
6. 点击某条需求下方的“我想合作”。
7. 勾选《中介服务协议》，填写姓名、手机号、单位和留言。
8. 点击“提交合作意向”。
9. 页面会生成查询码，例如 `TN-7FQ2M9XA`。请保存查询码，用户后续可在“进度查询”页面查看对接进度。

前台不会展示需求方联系方式，也不会展示 JSTEC 详情页链接。

## 3. 后台查看

进入“后台管理”页面，先登录管理员账号：

```text
账号：admin
密码：以你当前设置的管理员密码为准
```

正式给别人试用前，建议双击下面这个文件修改密码：

```text
修改TechNexus管理员密码.bat
```

登录后可以看到：

- 已读取的 JSTEC 技术需求数量
- 匹配记录数量
- 合作意向数量
- 来访者提交的姓名、手机号、单位、匹配需求、匹配分数和审核状态
- 合作意向状态流转：待审核、已联系成果方、已联系需求方、撮合中、已签中介协议、合作成功、合作失败
- 跟进备注
- 按状态、姓名、手机号、单位、需求名称、备注等关键词筛选
- 将当前筛选结果导出为 Excel
- 点击“查看”打开单条线索详情，查看完整留言、补充说明、AI 匹配理由、合作建议、协议版本和状态变更记录
- 单条线索详情里的后台进度表：打电话核实成果、联系需求方、发微信、约线上会议、发送协议、记录成交金额
- 对外进度说明：用户输入查询码后可看到
- 成交金额和成交备注：仅后台可见，不在用户查询页展示
- 需求库预览和搜索

所有后台数据会保存在 SQLite 数据库：

```text
C:\Users\xdh\Desktop\工作流\技术需求爬取\technexus_data\technexus.db
```

数据库里包含：

- 成果提交记录
- AI 匹配记录
- 合作意向记录
- 状态变更记录
- 查询码和对外进度说明
- 后台进度表
- 成交金额和成交备注

## 4. 停止服务

如果是双击启动窗口运行的，可以回到启动窗口，按：

```text
Ctrl + C
```

或直接关闭窗口。

如果服务在后台运行，可以双击：

```text
停止TechNexus试用版.bat
```

## 5. 当前版本说明

当前版本是本地试用版：

- 需求库来源于 `jstec_demands.checkpoint.jsonl`
- 已接入 DeepSeek API 做 AI 精排；如果 API 不可用，会自动退回本地规则匹配
- 已支持“快速匹配”和“AI智能匹配”两种模式
- 匹配结果默认展示 5 条，并用颜色区分匹配程度
- 已支持合作意向查询码，用户可查询对接进度
- 已支持后台进度表和对外进度说明
- 已按“技术领域、应用场景、产业方向、成熟度”四个维度打分
- 已加入管理员登录
- 已使用 SQLite 保存成果提交、匹配记录、合作意向和状态流转
- 后续可以继续增加小程序端和线上部署

## 6. DeepSeek 配置

配置文件在：

```text
C:\Users\xdh\Desktop\工作流\技术需求爬取\ai_config.json
```

当前使用：

```text
base_url: https://api.deepseek.com
model: deepseek-v4-pro
```

如需更换模型，修改 `ai_config.json` 中的 `model` 后，重启服务即可。

如需临时关闭 AI 精排，把：

```json
"enabled": true
```

改成：

```json
"enabled": false
```

关闭后系统会继续使用本地规则匹配。

不要把 `ai_config.json` 发给别人，里面包含你的 DeepSeek API Key。
