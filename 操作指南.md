# JSTEC 技术需求爬取脚本操作指南

本指南适用于当前目录里的脚本：

- `scrape_jstec_demands.py`
- `requirements.txt`

脚本会爬取江苏省技术产权交易市场技术需求信息，并导出为 Excel 表格。建议优先导出 `.xlsx`，不要直接用 `.csv`，因为 Excel 打开 CSV 时容易把长编号自动变成科学计数法。

## 1. 打开命令窗口

在 Windows 上使用 PowerShell。

进入脚本所在目录：

```powershell
cd "C:\Users\xdh\Documents\Codex\2026-05-29\python-https-www-jstec-com-cn"
```

确认当前目录里能看到脚本：

```powershell
dir
```

应能看到：

- `scrape_jstec_demands.py`
- `requirements.txt`

## 2. 安装依赖

第一次运行前执行一次：

```powershell
pip install -r requirements.txt
```

如果提示 `pip` 找不到，可以试：

```powershell
python -m pip install -r requirements.txt
```

## 3. 先做小样本测试

建议先抓 10 条，确认表格格式正常：

```powershell
python scrape_jstec_demands.py --limit 10 --delay 0 --output test_jstec_demands.xlsx
```

运行完成后，当前目录会生成：

```text
test_jstec_demands.xlsx
```

用 Excel 打开后检查：

- `需求编号` 是否完整显示
- `需求ID` 是否完整显示
- `联系方式` 是否完整
- `需求详情` 是否正常换行

## 4. 抓取全部数据

确认小样本没问题后，运行：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx
```

脚本会分页抓取列表，再逐条进入详情接口获取字段。

输出文件：

```text
jstec_demands.xlsx
```

脚本会自动生成断点文件：

```text
jstec_demands.checkpoint.jsonl
```

如果有网站接口自身报错、被跳过的单条记录，脚本还会生成：

```text
jstec_demands.skipped.log
```

这个文件用于记录需要后续人工复查的位置。

如果中途报错、断网或窗口关闭，不需要从头开始。直接重新运行同一条命令即可：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx
```

脚本会读取断点文件，跳过已经写入的需求，并从断点附近继续抓取。

如果脚本升级后新增了字段，比如“意向投入”，旧断点里的历史记录可能没有这个字段。可以先执行一次回补：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx --backfill-intended-price
```

回补完成后，再继续正常抓取：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx
```

如果你想完全重新抓取，使用：

```powershell
python scrape_jstec_demands.py --fresh --output jstec_demands.xlsx
```

## 5. 推荐的输出字段

Excel 中包含这些列：

- 需求名称
- 需求编号
- 合作方式
- 意向投入
- 联系方式
- 发布者
- 需求详情
- 技术领域
- 需求类型
- 所在地区
- 需求ID
- 详情页链接

其中 `需求编号`、`联系方式`、`需求ID` 已按文本格式写入 Excel，不会再被 Excel 转成科学计数法。

## 6. 登录模式

普通模式目前已经能从接口里优先取完整手机号。如果你后续发现某些详情必须登录后才能看到，可以使用登录模式。

运行：

```powershell
python scrape_jstec_demands.py --login --output jstec_demands.xlsx
```

脚本会提示你输入：

- 用户名或手机号
- 密码
- 验证码

验证码图片会保存到当前目录，默认文件名：

```text
jstec_captcha.png
```

打开这张图片，看清验证码后，在 PowerShell 里输入验证码即可。

也可以在命令里指定用户名：

```powershell
python scrape_jstec_demands.py --login --username "你的账号" --output jstec_demands.xlsx
```

不建议把密码直接写在命令里，因为命令历史可能会保存密码。让脚本提示输入更安全。

## 7. 常用参数

只抓前 100 条：

```powershell
python scrape_jstec_demands.py --limit 100 --output jstec_demands_100.xlsx
```

从第 5 页开始抓：

```powershell
python scrape_jstec_demands.py --start-page 5 --output jstec_demands_from_page5.xlsx
```

同时输出 JSONL：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx --jsonl jstec_demands.jsonl
```

降低请求速度，避免访问太快：

```powershell
python scrape_jstec_demands.py --delay 1 --output jstec_demands.xlsx
```

查看全部参数：

```powershell
python scrape_jstec_demands.py --help
```

## 8. 注意事项

1. 建议保持每页 `10` 条，不要调大 `--page-size`。网站接口目前调大后可能返回服务器错误。
2. 建议输出 `.xlsx`，不要用 `.csv` 给 Excel 直接打开。
3. 如果中途网络失败，直接重新运行同一条命令即可，脚本会自动读取断点文件续跑。
4. 抓取全部数据需要一些时间，窗口不要关闭。
5. 如果网站接口规则变化，脚本可能需要同步调整。
6. 部分列表页如果返回 400，脚本会自动拆成单条模式继续抓取；如果某一个单条位点仍然返回 400，会记录到 `.skipped.log` 并继续，避免整批任务中断。

## 9. 常见问题

### Excel 里编号变成 `6.8519E+15`

请使用 `.xlsx` 输出：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx
```

不要用 Excel 直接打开 CSV。

### 提示缺少 openpyxl

执行：

```powershell
pip install -r requirements.txt
```

或：

```powershell
python -m pip install -r requirements.txt
```

### 单条详情接口返回 500

如果运行时看到类似：

```text
getDemandDetail ... 500 Server Error
```

这通常是 JSTEC 网站某一条需求详情接口异常，不是本地程序坏了。脚本现在会在多次重试后：

1. 把失败信息写入 `jstec_demands.skipped.log`
2. 用列表页能拿到的信息先写入 Excel 和断点
3. 继续抓取后面的需求

如果你希望遇到详情失败时立刻暂停，可以额外加参数：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx --stop-on-detail-error
```

### 登录验证码怎么看

运行登录模式后，当前目录会出现：

```text
jstec_captcha.png
```

打开图片，把验证码输入 PowerShell。

### 想先确认脚本能不能跑

用小样本命令：

```powershell
python scrape_jstec_demands.py --limit 3 --delay 0 --output sample_check.xlsx
```

如果生成了 `sample_check.xlsx`，说明环境和脚本基本正常。

### 中途报错后如何续跑

例如你运行到 40 条时报错，不要删除 `.checkpoint.jsonl` 文件。直接重新运行原命令：

```powershell
python scrape_jstec_demands.py --output jstec_demands.xlsx
```

如果你的输出文件名是 `test_jstec_demands.xlsx`，就继续运行：

```powershell
python scrape_jstec_demands.py --output test_jstec_demands.xlsx
```

脚本会自动使用对应的 `test_jstec_demands.checkpoint.jsonl` 继续。
